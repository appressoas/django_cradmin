.. _listbuilder_models:

==============
Project Models
==============
In this guide we will create lists of songs which are on an album. Further is an album handled by an administrator.
Notice we use the Meta class for the Song model to get a capitol letter on Song, which is than shown in the template.
IF we don't do this we get song without a capitol letter in the template.
Our models look like this:

.. literalinclude:: /../django_cradmin/demo/cradmin_listbuilder_guide/models.py

Next chapter
------------
TODO